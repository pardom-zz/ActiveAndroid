apply plugin: 'java'
sourceCompatibility = 1.6
jar.baseName = 'activeandroid'

sourceSets {
	main {
		java {
			srcDir 'src'
		}
	}
}

dependencies {
	compile fileTree(dir: 'libs', include: '*.jar')
}